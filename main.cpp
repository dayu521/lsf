#include <iostream>
#include <fstream>
#include <memory>
//#include"kmp.h"

using namespace std;

int main()
{
//    setlocale(LC_ALL,"zh_CN.UTF-8");
    //std::locale("").name().c_str()
    std::locale::global(std::locale(""));
//    MBuff m("文本文件.txt");
//    m.next_char();
//    while (!m.is_eof()) {
//        auto ss=m.current_char();
//        std::wcout<<ss;
//        m.next_char();
//    }
//    std::wcout.flush();
    return 0;
}

